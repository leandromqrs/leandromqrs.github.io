---
import Hero from '../../components/Hero.astro';
import BaseLayout from '../../layouts/BaseLayout.astro';

// Paraglide translations
import { m } from "../../paraglide/messages.js";

// For dynamic routing
export function getStaticPaths() {
	return [
		{ params: { lang: 'en' } },
		{ params: { lang: 'pt-br' } }
	];
}
---

<BaseLayout title={m.notFoundTitle()} description={m.notFoundDescription()}>
	<Hero title={m.notFoundTitle()} tagline={m.notFoundTagline()} />
</BaseLayout>
